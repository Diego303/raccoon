<div class="hud">
    <div class="stat-box">
        <span class="label">TOTAL</span>
        <span id="total-score-val">0</span>
    </div>

    <!-- Center: Round or Mode Info -->
    <div class="stat-box" id="round-stat-box">
        <span class="label">ROUND</span>
        <span id="round-val">1/10</span>
    </div>
    
    <div class="stat-box" id="mode-display" style="display:none; color: var(--color-gold);">
        <span class="label">MODE</span>
        <span id="mode-val">ZEN</span>
    </div>

    <div class="stat-box">
        <span class="label">SCORE</span>
        <span id="score-val">0</span>
    </div>
</div>

</div>

<div style="text-align: center; color: var(--color-gold); font-size: 0.7rem; margin-top: 5px; margin-bottom: 2px;">
    CAFÉ RESTANTE - ESCRIBE O SE ACABARÁ!!!
</div>

<div class="timer-container" id="timer-container">
    <div class="timer-bar" id="timer-bar"></div>
</div>
<!-- Round Progress (Survival Time) -->
<div class="round-progress-container" id="round-progress-container" style="display:none; height: 4px; background: rgba(255,255,255,0.1); width: 100%; max-width: 600px; margin: 2px auto;">
     <div id="round-bar" style="height: 100%; background: var(--color-gold); width: 0%;"></div>
</div>

<style>
    .hud {
        display: flex;
        justify-content: space-between;
        align-items: center;
        width: 100%;
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        background: rgba(0,0,0,0.5);
        border: 4px solid var(--color-gold);
        border-radius: 8px;
    }

    .stat-box {
        display: flex;
        flex-direction: column;
        align-items: center;
        font-size: 1.2rem;
    }

    .label {
        font-size: 0.8rem;
        color: var(--color-gold);
        margin-bottom: 5px;
    }

    .timer-container {
        flex-grow: 1;
        height: 20px;
        background: #333;
        margin: 0 20px;
        border: 2px solid #fff;
        position: relative;
    }

    .timer-bar {
        width: 100%;
        height: 100%;
        background: var(--color-correct);
        transition: width 0.1s linear, background-color 0.5s;
    }
    
    .timer-bar.low {
        background: var(--color-wrong);
    }
    
    @media (max-width: 768px) {
        .hud {
            padding: 10px;
            flex-wrap: wrap; /* Allow wrapping if needed, but best to keep row */
            border-width: 2px;
        }
        
        .stat-box {
            font-size: 0.9rem;
        }
        
        .label {
            font-size: 0.6rem;
        }
        
        /* Hide non-essential if really small? No, all are essential. */
    }
</style>
